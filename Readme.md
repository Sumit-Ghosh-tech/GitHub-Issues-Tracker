# GitHub-Issues-Tracker
<h1>Question and Answer</h1>
1. What is the difference between var, let, and const?
   Answer: "var" is function-scoped and can be re-declared, "let" is block-scoped and can be updated, and "const" is block-scoped but cannot be updated or re-declared.

